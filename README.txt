OpenAlea.Deploy

Install and deploy OpenAlea packages.

It defines :
  - setuptool extension
  
  setup keywords:
     
     create_namespace = True, : create directory with __init__ corresponding to namespace

     # EGG-INFO
     shared_lib = ['lib'], 
     shared_include = ['include'],
     shared_data = [],

     include_package_data = True # include also not versionned data

